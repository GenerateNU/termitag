Vendored from [dmmulroy/anti-slop](https://github.com/dmmulroy/anti-slop) at
commit `c44ef22ca116d0ba62a3ff663a0bd13a3f3fa40b`.

Only the generic rule set (`src/`) is vendored — the Effect-specific rules
(`src/effect/`) are omitted since this project doesn't use Effect.

This is vendored source, not an npm dependency: there's no version to bump.
To update, re-copy `src/` from upstream and diff against local changes.
